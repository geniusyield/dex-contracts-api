module GeniusYield.Scripts.Dex.Data (
  orderValidator,
  nftPolicy,
) where

import           GeniusYield.OnChain.Common.Scripts.DEX.Data (nftPolicy,
                                                              orderValidator)
